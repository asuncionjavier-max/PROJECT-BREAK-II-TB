import app from "./app.js"
const PORT = 3000;

app.listen(PORT, () =>{
    try {
        console.log(`Servidor funcionando en puerto${PORT} `)
        
    } catch (error) {
        
        
    }
})